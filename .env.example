# PostgreSQL / Supabase connection string.
# Keep the real password only in .env or Fly.io secrets.
DATABASE_URL=postgresql://postgres.<project-ref>:<your-supabase-password>@<pooler-host>:5432/postgres